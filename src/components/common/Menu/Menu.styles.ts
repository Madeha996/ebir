import styled from "styled-components";
import { Menu as AntMenu, MenuProps } from "antd";
import React from "react";

export const Menu = styled(
  AntMenu as unknown as React.ComponentType<MenuProps>
)``;
